using System;

namespace Robots.Commands
{
    /// <summary>
    /// a command list
    /// </summary>
    public enum RobotCommand
    {
        PLACE = 1,
        MOVE = 2,
        LEFT = 3,
        RIGHT = 4,
        REPORT = 5
    }
}
